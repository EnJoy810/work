# 手动留痕批注缩放方案备忘

## 背景
- 项目：`qingjing-grader-frontend`（React + Vite）。
- 手动留痕主页面在 `src/pages/manual-review`，但缩放交互可先在 `src/pages/trace-demo` 调试。
- 现有批注只支持拖拽，宽度由内容自适应（`max-width: 260px`），高度随文本行数。
- 目标：实现“按住批注并滚动鼠标滚轮即可缩放”，默认等比，结果在统一保存时提交。

## 核心需求
1. 交互：选中批注按住鼠标左键，滚轮上/下分别放大/缩小；松开后恢复页面滚动。
2. 缩放范围：推荐 0.7~1.6（70%~160%），步长 0.1 或 0.05。
3. 只支持等比缩放，每次操作一个批注；缩放值需持久化。
4. 需有提示或百分比显示，帮助老师发现并看懂当前大小。
5. 后端尚未支持 `scale` 字段，前端需要 mock；后续后端上线后直接写入。

## 数据与状态
- 批注对象新增 `scale: number` 字段，默认 1。
- 加载数据时若缺省，自动补 1；Mock 数据 (`src/pages/trace-demo/mockData.js`) 中可加入不同 `scale` 以验证效果。
- 保存动作中携带 `scale`，即使暂未发送到后端，也要在本地记录，方便后续接入。

## 交互实现
1. **AnnotationCard.jsx**
   - `mousedown`：设置 `isMouseDown = true`，并注册 `window.addEventListener('wheel', handleWheel, { passive: false })`。
   - `mouseup` / `mouseleave`：重置 `isMouseDown` 并移除 `wheel` 监听。
   - `handleWheel`：若 `isMouseDown`，调用 `event.preventDefault()` 阻止页面滚动；根据 `deltaY` 判定缩放方向（上滚放大、下滚缩小），`scale = clamp(scale ± step, min, max)`；通知父组件 `onScaleChange(annotationId, nextScale)` 更新状态。
2. **拖拽兼容**：`dragStartRef` 记住 `scale`，拖拽位移需除以当前 `scale`，确保缩放后拖动仍精准。
3. **渲染**：卡片容器使用 `transform: translate(-50%, -50%) scale(scale)`，配合 `transform-origin: center`，保持以中心缩放。
4. **样式**：可固定基准宽度（比如 220px）再由 `scale` 控制视觉尺寸；保持 `word-break`，避免文字被裁切。
5. **提示反馈**：选中或按住批注时显示提示“按住并滚轮可缩放”，缩放时可展示当前百分比，如 `120%`。

## 前端待办
- [ ] 数据结构：批注新增 `scale`，默认 1；state、接口、保存逻辑全部带上。
- [ ] Mock：`mockData.js` 中为部分批注设定不同 `scale` 以观察效果。
- [ ] 交互：`AnnotationCard` 实现 `mousedown + wheel` 缩放，阻止默认滚动，更新 state。
- [ ] 渲染：批注容器使用 `translate + scale`，拖拽算法考虑 `scale`，必要时微调 CSS。
- [ ] 提示：添加可见提示和缩放百分比显示，增强可发现性。
- [ ] 保存：统一保存时把 `scale` 一并提交，后端上线后直接落库。

## 后端协作要点
- 模型/接口增加 `scale`（浮点数，默认 1），读写均支持。
- 兼容旧数据：缺少 `scale` 时返回 1。
- 前端会传 0.7~1.6 内的数值，后端只需存储即可。
