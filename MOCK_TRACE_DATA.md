### 数据结构
- `MOCK_STUDENTS`：静态学生列表（id、姓名、学号、状态、grading_id）。
- `MOCK_ANSWER_SHEETS`：以学生 id 为 key 的答题卡数据：
  - `paper_urls`：正反面图片地址数组。
  - `questions`：题目列表；每题包含：
    - `paper_id`：`ai-xxx-000n`，用于归类正反页面。
    - `bbox`：题目在整页上的相对矩形（0–1 坐标）。
    - `rtp`：推荐落点相对于 `bbox` 的比例坐标。
    - `annotations`：批注内容、来源、原始/当前坐标等。
### 计算流程
1. **先拿到整套 mock 数据**：
页面初始化会按学生 id 读 `MOCK_ANSWER_SHEETS`，里面已经包含答题卡 URL、每道题的矩形范围 (`bbox`) 以及默认批注落点 (`rtp`)。
2. **量出图片当前的真实宽高**：
图片渲染出来后，前端量一下此刻在屏幕上占了多宽、多高（例如 1200px × 850px），这个数据后面要做乘法。
3. **把 `bbox` 换成像素框**：
`bbox` 给的是“相对整张卷面”的比例（比如 `x:0.30` 表示在卷面横向 30% 的位置）。
前端直接做乘法：`left = 0.30 * 1200px = 360px`，`width = 0.20 * 1200px = 240px`，纵向同理，于是就得到题目在屏幕上的矩形位置。
4. **把批注推荐点 (`rtp`) 也乘回像素**：
`rtp` 是相对该题矩形的比例点，先加上题目左上角，再乘当前宽高。
例如推荐点 `x:0.8`，题目左边在 360px，题目宽 240px，则批注横坐标 = `360 + 0.8 * 240 = 552px`。纵向用同样的加法和乘法，最终得到批注在画布上的具体位置。
5. **拖拽时反向算回比例**：
老师拖动批注时，前端获得鼠标在屏幕上的像素坐标，然后用除法复原成比例：`pageRatioX = pixelX / imageWidth`，`rtp.x = (pageRatioX - bbox.x) / bbox.width`。
这样无论用户缩放或窗口大小怎么变，都能还原成 0–1 的相对坐标存起来，后端也能直接使用。

